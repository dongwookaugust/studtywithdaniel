//clone - cloning the entire history of the remote repository to your local machine
//commit -unit that records the current state of a project in a repository
//push & pull - Used to synchronize changes between local and remote repositories
//merge - Consolidate changes from two or more branches into one
//resolve conflict  - occurs when the same part of the same file is modified differently in two branches.
//branchion -
